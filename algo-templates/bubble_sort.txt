\Fn BubbleSort(A, n) {
    \For (i \gets 0; i < n - 1; i \gets i + 1) {
        \For (j \gets 0; j < n - i - 1; j \gets j + 1) {
            \If (A[j] > A[j+1]) {
                temp \gets A[j];
                A[j] \gets A[j+1];
                A[j+1] \gets temp;
            }
        }
    }
}
